# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Cloudflare Wrangler local state + build artifacts (deploy-only, not source)
/.wrangler/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Stray nested clone
/kra/

# Local secrets. These were dropped from this branch's ignore list, which left
# .env / .dev.vars files untracked-but-not-ignored — one `git add -A` from being
# committed. The negations keep the checked-in examples visible.
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Kotlin incremental-compile session files (build artifacts, not source).
android/.kotlin/
